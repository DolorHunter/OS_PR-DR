#ifndef PAGESREPLACEMENT_MAIN_H
#define PAGESREPLACEMENT_MAIN_H

#include <stdio.h>
#include "src/pagesReplacement.h"
#include "src/file.h"
#include "src/timer.h"

int pickPlay(int casePlay);
int pickReplacement(int caseReplacement);
int pickTimer(int caseTimer);

int main();

#endif //PAGESREPLACEMENT_MAIN_H
